# Gamma Cohort Boilerplate #

## Artifactory ##
Comment out hootsuite artifactorylines in ~/.pip/pip.config so you can install external dependencies

## Install Requirements ##
`pip install -r requirements.txt`

## Run examples ##
`python validation.py`